#!/usr/bin/env node
/**
 * `yatt-ts` command line entry point.
 *
 * Placeholder — full CLI (serve/--version/flags) lands with task T10.
 */
import { VERSION } from './index.js';

process.stdout.write(`yatt-ts ${VERSION}\n`);
